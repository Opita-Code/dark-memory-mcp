name: build-mcpb

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write  # For npm provenance (in case we publish to npm from here)

jobs:
  build:
    name: Build ${{ matrix.target }} binary
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: darwin-amd64
            goos: darwin
            goarch: amd64
            binary-name: dark-mem-mcp-amd64
            bundle-name: darwin
            ext: ""
          - target: darwin-arm64
            goos: darwin
            goarch: arm64
            binary-name: dark-mem-mcp-arm64
            bundle-name: darwin
            ext: ""
          - target: linux-amd64
            goos: linux
            goarch: amd64
            binary-name: dark-mem-mcp-amd64
            bundle-name: linux
            ext: ""
          - target: linux-arm64
            goos: linux
            goarch: arm64
            binary-name: dark-mem-mcp-arm64
            bundle-name: linux
            ext: ""
          - target: windows-amd64
            goos: windows
            goarch: amd64
            binary-name: dark-mem-mcp-amd64
            bundle-name: win32
            ext: ".exe"
          - target: windows-arm64
            goos: windows
            goarch: arm64
            binary-name: dark-mem-mcp-arm64
            bundle-name: win32
            ext: ".exe"

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Extract version from git tag
        id: version
        run: |
          VERSION="${GITHUB_REF#refs/tags/v}"
          echo "VERSION=${VERSION}" >> "$GITHUB_OUTPUT"
          # Commit timestamp (deterministic per tag) for SOURCE_DATE_EPOCH
          # so PE/Mach-O/ELF header timestamps match across all publish
          # workflows (npm + GitHub Release) -> reproducible SHAs.
          echo "EPOCH=$(git log -1 --format=%ct)" >> "$GITHUB_OUTPUT"

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          # Pin exact Go patch version (not just "1.25") so publish-npm
          # and build-mcpb produce byte-identical binaries. With "1.25"
          # setup-go resolves to whatever 1.25.x.y is the latest at the
          # time of each runner's install; if those resolve differently
          # on the two parallel runners, the SHA256 of the same source
          # built with the same ldflags will drift. Pinning to 1.25.12
          # guarantees both runners get the exact same toolchain.
          go-version: "1.25.12"

      - name: Cross-compile dark-mem-mcp binary for ${{ matrix.target }}
        working-directory: cmd/dark-mem-mcp
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
          # SOURCE_DATE_EPOCH pins the PE/Mach-O/ELF header timestamp to
          # the commit timestamp (deterministic). Without this, each build
          # embeds the current wall clock time, causing cross-publish SHA
          # drift even when Go version + ldflags match.
          # https://reproducible-builds.org/docs/source-date-epoch/
          SOURCE_DATE_EPOCH: ${{ steps.version.outputs.EPOCH }}
        run: |
          set -euo pipefail
          mkdir -p ../../build/server-${{ matrix.bundle-name }}
          go build \
            -trimpath \
            -ldflags "-s -w -X github.com/dark-agents/dark-memory-mcp/internal/version.buildVersion=${{ steps.version.outputs.VERSION }}" \
            -o ../../build/server-${{ matrix.bundle-name }}/${{ matrix.binary-name }}${{ matrix.ext }} \
            .

      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          # Use bundle-name (not target) so the glob `binary-<bundle>-*` matches.
          # win32 would otherwise break because target is `windows-*` but bundle-name is `win32`.
          name: binary-${{ matrix.bundle-name }}-${{ matrix.goarch }}
          path: build/server-${{ matrix.bundle-name }}/${{ matrix.binary-name }}${{ matrix.ext }}
          retention-days: 1

  bundle:
    name: Build ${{ matrix.bundle-name }} .mcpb bundle
    runs-on: ubuntu-latest
    needs: build
    strategy:
      fail-fast: false
      matrix:
        bundle-name:
          - darwin
          - linux
          - win32
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Download all binaries for ${{ matrix.bundle-name }}
        uses: actions/download-artifact@v4
        with:
          path: build/${{ matrix.bundle-name }}
          pattern: binary-${{ matrix.bundle-name }}-*
          merge-multiple: true

      - name: Assemble bundle directory structure
        run: |
          set -euo pipefail
          # Copy manifest
          cp mcpb/manifest.json build/${{ matrix.bundle-name }}/manifest.json
          # Move binaries from build/server-${{ matrix.bundle-name }}/ to build/${{ matrix.bundle-name }}/server/
          mkdir -p build/${{ matrix.bundle-name }}/server
          # Find and move both arch binaries
          for arch in amd64 arm64; do
            # Darwin and Linux binaries have no extension; Windows has .exe
            for ext in "" ".exe"; do
              src="build/${{ matrix.bundle-name }}/dark-mem-mcp-${arch}${ext}"
              if [ -f "$src" ]; then
                mv "$src" "build/${{ matrix.bundle-name }}/server/dark-mem-mcp-${arch}${ext}"
                echo "Moved $src to server/"
              fi
            done
          done
          echo "=== Final bundle structure for ${{ matrix.bundle-name }} ==="
          find build/${{ matrix.bundle-name }} -type f -exec ls -la {} \;

      - name: Zip .mcpb bundle
        run: |
          set -euo pipefail
          cd build
          # Use zip without compression on the binaries (already compressed by Go build)
          zip -0 -r "../dark-memory-mcp-${{ matrix.bundle-name }}.mcpb" "${{ matrix.bundle-name }}/" > /dev/null
          cd ..
          ls -la dark-memory-mcp-${{ matrix.bundle-name }}.mcpb

      - name: Verify bundle contents
        run: |
          echo "=== Bundle file listing ==="
          unzip -l dark-memory-mcp-${{ matrix.bundle-name }}.mcpb
          echo ""
          echo "=== Bundle manifest.json head ==="
          unzip -p dark-memory-mcp-${{ matrix.bundle-name }}.mcpb ${{ matrix.bundle-name }}/manifest.json | head -c 500

      - name: Upload .mcpb artifact
        uses: actions/upload-artifact@v4
        with:
          name: bundle-${{ matrix.bundle-name }}
          path: dark-memory-mcp-${{ matrix.bundle-name }}.mcpb
          retention-days: 30

  attach-to-release:
    name: Attach .mcpb bundles + raw binaries to GitHub Release
    runs-on: ubuntu-latest
    needs: [build, bundle]
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write  # Required to create / update GitHub Releases via softprops/action-gh-release@v2
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Download all .mcpb artifacts
        uses: actions/download-artifact@v4
        with:
          path: .
          pattern: bundle-*
          merge-multiple: true

      - name: Download all raw binary artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-binaries
          pattern: binary-*
          # NOTE: do NOT set merge-multiple: true here. With merge-multiple,
          # all matching artifacts are extracted into a single directory with
          # NO subdirectories — which means files with the same name COLLIDE
          # (e.g. dark-mem-mcp-amd64 from darwin and linux binaries). One
          # would silently overwrite the other, so the rename script below
          # only finds 4 files instead of 6.
          #
          # With merge-multiple omitted (default = false), each artifact is
          # downloaded to its own subdirectory named after the artifact
          # (binary-darwin-amd64/, binary-linux-amd64/, ...). The rename
          # script then flattens to release-binaries/<bin>-<bundle>.

      - name: Rename raw binaries for release upload
        working-directory: release-binaries
        run: |
          # The build job uploaded each binary into a subdirectory named
          # `binary-<bundle-name>-<arch>` (e.g. binary-darwin-amd64).
          # Inside that dir is the binary itself (e.g. dark-mem-mcp-amd64).
          # Flatten to `release-binaries/<binary>-<bundle-name>` so the
          # attach-to-release files: glob matches.
          #
          # IMPORTANT: cwd is `release-binaries/` (set by working-directory),
          # so paths in this script are RELATIVE to that. The files glob in
          # the attach-to-release step is resolved relative to the JOB root
          # (workspace), so files end up at `release-binaries/<binary>-<bundle>`
          # from the workspace root.
          shopt -s globstar nullglob
          for src in **/*; do
            [ -f "$src" ] || continue
            base=$(basename "$src")
            target_dir=$(dirname "$src")
            bundle_arch=$(basename "$target_dir")
            # Strip "binary-" prefix and arch suffix to extract bundle-name.
            # e.g. binary-darwin-amd64 -> darwin
            if [[ "$bundle_arch" =~ ^binary-([^-]+)-.+$ ]]; then
              bundle_name="${BASH_REMATCH[1]}"
              # target is RELATIVE to cwd (which is release-binaries/).
              # From the workspace root, the file ends up at
              # `release-binaries/<binary>-<bundle>` which matches the
              # attach-to-release files: glob.
              target="${base}-${bundle_name}"
              [ -e "$target" ] && continue
              mv "$src" "$target"
            fi
          done
          # Emit summary
          echo "=== Binaries ready for release ==="
          ls -la

      - name: Find or create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          generate_release_notes: true
          files: |
            dark-memory-mcp-darwin.mcpb
            dark-memory-mcp-linux.mcpb
            dark-memory-mcp-win32.mcpb
            release-binaries/dark-mem-mcp-amd64-darwin
            release-binaries/dark-mem-mcp-arm64-darwin
            release-binaries/dark-mem-mcp-amd64-linux
            release-binaries/dark-mem-mcp-arm64-linux
            release-binaries/dark-mem-mcp-amd64.exe-win32
            release-binaries/dark-mem-mcp-arm64.exe-win32
            skills/dark-memory/SKILL.md
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
